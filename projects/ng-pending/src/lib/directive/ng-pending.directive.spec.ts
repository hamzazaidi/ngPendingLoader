import { NgPendingDirective } from './ng-pending.directive';

describe('NgPendingDirective', () => {
  it('should create an instance', () => {
    const directive = new NgPendingDirective();
    expect(directive).toBeTruthy();
  });
});
